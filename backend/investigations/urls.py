from django.urls import path
from rest_framework.routers import DefaultRouter

# NOTE: views will be added in follow-up commits

router = DefaultRouter()

urlpatterns = [
    # placeholder for manual case-nested endpoints
] + router.urls
